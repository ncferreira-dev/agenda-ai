# Blueprint do Render: cria o backend (Docker) + um Postgres e liga tudo sozinho.
# O Render gera JWT_SECRET/CPF_HASH_SECRET (generateValue) e injeta a DATABASE_URL
# do banco criado. Este arquivo é a fonte da verdade pro Blueprint: qualquer edição
# manual de env var no dashboard que também exista aqui é sobrescrita a cada sync
# (deploy por push). Por isso a URL da Vercel fica fixada aqui, não só no dashboard.
# As migrations rodam no start (docker-entrypoint.sh -> prisma migrate deploy).
databases:
  - name: agendai-db
    plan: free
    databaseName: agenda
    user: agenda

services:
  - type: web
    name: agendai-api
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agendai-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CPF_HASH_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      # Origem pública do site na Vercel (pra onde o navegador volta após o OAuth
      # e o CORS libera). Se o domínio da Vercel mudar, atualiza aqui.
      - key: WEB_ORIGIN
        value: https://agenda-ai-alpha.vercel.app
      # URL pública desta própria API no Render (ex.: https://agendai-api.onrender.com).
      # Obrigatória em produção quando o Google está configurado (vira o callback do OAuth).
      - key: PUBLIC_API_URL
        sync: false
      # Login social do dono — Google (opcional). Credenciais criadas em
      # console.cloud.google.com (OAuth 2.0, Aplicativo da Web). Redirect autorizado:
      #   <PUBLIC_API_URL>/auth/oauth/google/callback
      # sync: false = preenchido manualmente no dashboard do Render, nunca no git.
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
